<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- 终止引擎专用测试目标：只供 Tests 启动与结束，不进入正式发布包 -->
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AssemblyName>CuinProcessEase.TerminationTestApp</AssemblyName>
    <RootNamespace>CuinProcessEase.TerminationTestApp</RootNamespace>
  </PropertyGroup>

</Project>
